<UserControl
    x:Class="Attendance.WPF.Views.UserMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:clock="clr-namespace:Attendance.WPF.Clock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Attendance.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Grid.Column="0"
            Margin="0,0,5,0"
            Background="FloralWhite"
            BorderBrush="Black"
            BorderThickness="1"
            CornerRadius="5">
            <local:UserSelectActivityView Margin="10" DataContext="{Binding UserSelectActivityViewModel}" />
        </Border>

        <Border
            Grid.Column="1"
            Margin="5,0,10,0"
            Background="FloralWhite"
            BorderBrush="Black"
            BorderThickness="1"
            CornerRadius="5">
            <local:UserDailyOverviewView Margin="10" DataContext="{Binding UserDailyOverviewViewModel}" />
        </Border>
    </Grid>
</UserControl>
